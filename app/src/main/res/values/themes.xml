<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NJCourts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/pats_status_bar</item>
        <!-- For light status bars (dark icons) -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.NJCourts" parent="Base.Theme.NJCourts" />
</resources>